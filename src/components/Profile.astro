---
import { profileConfig } from "../config";
import ImageModule from "./misc/ImageModule.astro"
import IconModule from './misc/IconModule.astro'

const config = profileConfig;
---

<div class="profile">
    <div class="avatar">
        <ImageModule src={profileConfig.avatar} alt="MaxLeton 的头像" class="image" width={100} height={100}/>
    </div>
    <div class="text"> 
        <div class="name">{config.name}</div>
        <div class="discription">{config.description}</div>
    </div>
    
    <div class="links">
        {config.links.map(item => (
            <div class="link">
                <a 
                  href={item.url} 
                  aria-label={item.name}
                  {...item.url.startsWith('/') 
                    ? {} 
                    : { target: "_blank", rel: "noreferrer" }} 
                  data-title={item.name}
                >
                    <IconModule icon={item.icon} size={25} color={item.color}/>
                </a>
            </div>
        ))}
    </div>
</div>

<style>
.profile {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    flex: 1;
    width: 100%;
    max-width: 100%;
    padding: 20px 16px;
    margin-top: clamp(4rem, 18vh, 25vh);
}
.text{
    display: block;
    text-align: center;
    margin-top: 10px;
    /* font-family: 'Delius'; */
}

.text .name{
    font-size: 1.5rem;
    /* font-weight: bold; */
    margin-bottom: 0.5rem;
    
}
.text .discription{ 
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    color: var(--motto-text-color);
}
.links{ 
    border-top: var(--border-color) solid 1px;
    display: flex;
    margin-top: 0.5rem;
    padding-top: 0.5rem;
}
.links div {
    margin: 10px;
}

/* 自定义提示框样式 */
.link {
    position: relative;
}

.link a {
    position: relative;
    display: inline-block;
    opacity: 0.8;
    transition: opacity 0.3s ease;
}

.link a:hover {
    opacity: 1;
}

.link a:focus-visible {
    outline: 2px solid var(--link-color);
    outline-offset: 4px;
    opacity: 1;
}

.link a::after {
    content: attr(data-title);
    position: absolute;
    bottom: -40px;
    left: 50%;
    transform: translateX(-50%);
    background-color: #333;
    color: white;
    padding: 6px 12px;
    border-radius: 6px;
    font-size: 14px;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
    z-index: 10;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

.link a:hover::after {
    opacity: 1;
    visibility: visible;
}
</style>

<!-- ImageModule 的 css 样式需要设置为 is:global 才能应用 -->
<style is:global>
.avatar { 
    display: block;
    text-align: center;
}
.avatar .image{
    width: 100px !important;
    height: 100px !important;
    object-fit: cover;
    border-radius: 50%;
    overflow: hidden;
    box-shadow: 0 4px 20px var(--image-shadow-color);
}
</style>